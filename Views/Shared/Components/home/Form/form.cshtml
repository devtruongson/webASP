@{
    var data = ViewBag.location as List<AllCodeDTO>;
}

<div class="form-home-wp">
    <div class="overlay"></div>
    <div class="container" style="height: 100%;">
        <div class="wp-form-home">
            <div class="row">
                <div class="col-6">
                    <div class="d-flex justify-content-center align-items-center" style="height: 100%;">
                        <div>
                            <h1>Dịch vụ thuê xe máy</h1>
                            <p>Chúng tôi hiểu cảm giác của người đi thuê xe máy phải bỏ ra một số tiền để có được một
                                chiếc
                                xe
                                đủ
                                tốt, không gặp rắc rối khi đi trên đường. Tuy nhiên thật khó để tìm được một đơn vị cung
                                cấp
                                dịch vụ
                                cho thuê xe uy tín, chất lượng hiện nay. MOTOGO - tiên phong trở thành đơn vị số 1 về
                                cung
                                cấp
                                dịch
                                vụ thuê xe máy tự lái tại Việt Nam.</p>
                        </div>
                    </div>
                </div>
                <div class="col-6">
                    <div class="form-wp-right">
                        <form asp-action="SubmitBookingForm" asp-controller="Home" method="post">
                            <div>
                                <h3>THÔNG TIN NHẬN XE</h3>
                                <div class="mb-2">
                                    <select name="LocationId" id="LocationId" class="form-control" required>
                                        <option value="">Chọn điểm nhận xe</option>
                                        @foreach (var item in data)
                                        {
                                            <option value="@item.id">@item.content_title </option>
                                        }
                                    </select>
                                </div>
                                <div class="mb-2">
                                    <p style=" margin-bottom: 0; font-weight: 400;">NGÀY NHẬN XE</p>
                                    <input type="date" name="FirstReceived" class="form-control"
                                        placeholder="Ngày nhận xe" required>
                                </div>
                            </div>
                            <div class="mt-3">
                                <h3>THÔNG TIN TRẢ XE</h3>
                                <div class="mb-3">
                                    <p style="margin-bottom: 0; font-weight: 400; ">NGÀY TRẢ XE</p>
                                    <input type="date" name="DateReturnCar" class="form-control"
                                        placeholder="Ngày trả xe" required>
                                </div>
                                <div class="mb-3">
                                    <input type="number" name="CustomerId" class="form-control"
                                        placeholder="Mã khách hàng" required>
                                </div>
                                <div class="mb-3">
                                    <input type="number" name="phoneNumber" class="form-control"
                                        placeholder="Số điện thoại" required>
                                </div>
                                <div class="mb-3">
                                    <button class="btn btn-warning w-100 py-2" style="font-weight: 600;">NHẬN
                                        TIN</button>
                                </div>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
